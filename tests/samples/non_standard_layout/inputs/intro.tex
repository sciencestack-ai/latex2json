This is the introduction section loaded from inputs/intro.tex.

The file is located at the project root level in the inputs/ folder,
while main.tex is in the samples/ folder.

This tests that \texttt{\\input} commands can resolve files relative
to the project root, not just relative to the main tex file's directory.
