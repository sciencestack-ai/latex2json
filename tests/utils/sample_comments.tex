\begin{tikzpicture}
%\newcommand{\myFunc}{sin(deg(x))+cos(2*deg(x))*cos(3*deg(x))/6  +sin(2*deg(x))/10}
\begin{axis}[
    axis line style={draw=none}, 
    disabledatascaling, % sss
%    axis equal, 
    yticklabels={,,},     
    xticklabels={,,},
    tick style={draw=none},
]
\end{axis}
\end{tikzpicture}